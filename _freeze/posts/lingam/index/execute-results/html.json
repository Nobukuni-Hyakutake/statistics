{
  "hash": "89af7550b56c4b793942ef8f4f2b828b",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"lingam (因果推論)\"\nauthor: \"Nobukuni Hyakutake\"\ndate: \"2026-09-05\"\ndate-format: \"iso\"\ncategories: [Python]\nimage: \"field_mustard.jpeg\"\n---\n\n![](field_mustard.jpeg)\n\n## Lingam 例1\n\n::: {#4445261e .cell execution_count=1}\n``` {.python .cell-code}\nimport lingam\nfrom lingam.utils import make_dot\nimport numpy as np\nimport pandas as pd\n\n# 再現性のためのシード設定\nnp.random.seed(42)\nn_samples = 1000\n\n# 1. ノイズ（非ガウス分布）の生成\ne1 = np.random.uniform(-1, 1, n_samples)\ne2 = np.random.uniform(-1, 1, n_samples)\ne3 = np.random.uniform(-1, 1, n_samples)\n\n# 2. 因果構造の定義 (X1 -> X2, X1 -> X3, X2 -> X3)\nx1 = e1\nx2 = 2.0 * x1 + e2\nx3 = 0.5 * x1 - 1.5 * x2 + e3\n\ndf = pd.DataFrame({\"X1\": x1, \"X2\": x2, \"X3\": x3})\n\n# LiNGAMの実行\nmodel = lingam.DirectLiNGAM()\nmodel.fit(df)\n\n# df.columns を .tolist() でリスト化して渡します\ndot = make_dot(model.adjacency_matrix_, labels=df.columns.tolist())\n\n# 画像として保存（カレントディレクトリに causal_graph.png が生成されます）\ndot.render(\"causal_graph1\", format=\"png\")\nprint(\"正常にグラフ画像が出力されました\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n正常にグラフ画像が出力されました\n```\n:::\n:::\n\n\n![](causal_graph1.png)\n\n## Lingam 例2\n\n::: {#c53ca1fc .cell execution_count=2}\n``` {.python .cell-code}\n# 再現性のためのシード設定\nnp.random.seed(101)\n\n# 1. ノイズ（非ガウス分布）の生成\ne1 = np.random.uniform(-1, 1, n_samples)\ne2 = np.random.uniform(-1, 1, n_samples)\ne3 = np.random.uniform(-1, 1, n_samples)\ne4 = np.random.uniform(-1, 1, n_samples)\ne5 = np.random.uniform(-1, 1, n_samples)\ne6 = np.random.uniform(-1, 1, n_samples)\n\n# 2. 因果構造の定義 (X1 -> X2, X1 -> X3, X2 -> X3,X5 -> X6)\nx1 = e1\nx2 = 4.0 * x1 + e2\nx3 = 0.5 * x1 - 1.5 * x2 + e3\nx4=e4\nx5=e5\nx6= 0.8*x5+e6\n\ndf = pd.DataFrame({\"X1\": x1, \"X2\": x2, \"X3\": x3,\"X4\": x4,\"X5\": x5,\"X6\": x6})\n\n# LiNGAMの実行\nmodel = lingam.DirectLiNGAM()\nmodel.fit(df)\n\n# df.columns を .tolist() でリスト化して渡します\ndot = make_dot(model.adjacency_matrix_, labels=df.columns.tolist())\n\n# 画像として保存（カレントディレクトリに causal_graph.png が生成されます）\ndot.render(\"causal_graph2\", format=\"png\")\nprint(\"正常にグラフ画像が出力されました\")\n```\n\n::: {.cell-output .cell-output-stdout}\n```\n正常にグラフ画像が出力されました\n```\n:::\n:::\n\n\n![](causal_graph2.png)\n\n## Python version\n\n::: {#bac658cd .cell execution_count=3}\n``` {.python .cell-code}\nimport platform\n\"Python version \"+platform.python_version()\n```\n\n::: {.cell-output .cell-output-display execution_count=6}\n```\n'Python version 3.11.9'\n```\n:::\n:::\n\n\n",
    "supporting": [
      "index_files"
    ],
    "filters": [],
    "includes": {}
  }
}